# Pipeline outputs — never committed
reports/
_reports/
*.zip

# Local Azure CLI cache
.azure/

# Exported ARM/Bicep (generated artefacts — published via pipeline artifacts)
export-results/
scan-results/
dr-config/

# Temp files
*.tmp
*.bak
.DS_Store
Thumbs.db

# Local-only handoff / session notes — never commit
docs/NEXT-SESSION-BRIEF.md
docs/*-handoff.md
docs/*-scratch.md
